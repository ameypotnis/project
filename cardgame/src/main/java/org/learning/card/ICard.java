package org.learning.card;

/**
 * Contract to represent card 
 */
public interface ICard {
	
	/**
	 * Suit type with following values
	 * D = Diamonds, H = Hearts, C = Clubs, S = Spades
	 */
	public enum Suit {S, C, H, D};

}
